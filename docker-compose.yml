services:
  web:
    image: php:5.6-apache
    container_name: entrainement_ortho
    ports:
      - "8080:80"
    volumes:
      - .:/var/www/html
      - ./docker/php/php.ini:/usr/local/lib/php.ini
    environment:
      - APACHE_DOCUMENT_ROOT=/var/www/html